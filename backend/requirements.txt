# KWAC OS v2 — Python dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.30

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# AI
anthropic==0.25.1

# Data processing (XP engine, analytics, valuation — no AI needed)
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.4.2        # for KNN valuation model
openpyxl==3.1.2            # Excel import

# Google APIs (contacts sync)
google-auth==2.29.0
google-auth-oauthlib==1.2.0
google-api-python-client==2.128.0

# Email
aiosmtplib==3.0.1

# Scheduling
apscheduler==3.10.4

# Utilities
python-dotenv==1.0.1
httpx==0.27.0              # async HTTP client
pydantic==2.7.1            # data validation (built into FastAPI)
pydantic-settings==2.2.1

# Geo (distance calculations for valuation)
geopy==2.4.1
